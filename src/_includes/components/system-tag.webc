<script webc:type='js'>
	const internalClass = this.class || '';

	const renderNumsTag = (numsTag) => {
		if (!numsTag) return '';
		return `
			<span class="text-base text-slate-500">${numsTag}</span>
		`
	}

	`
		<span class='text-sm font-medium lowercase text-primary-400 cursor-pointer hover:text-primary-600 ${internalClass}'>
			<a class="before:content-['#'] before:mr-[2px]" @attributes="({ href: '${url}' })">${tag}</a>
			${renderNumsTag(count)}
		</span>
	`
</script>
